# AllergyFree 개발 진행 및 변경사항 요약

지금까지 진행한 **AllergyFree 앱**의 프론트엔드 최적화, 백엔드 서버 기능 연동 및 기능 개선 작업에 대한 종합 정리입니다.

---

## 🎨 1. 프론트엔드 최적화 및 리팩토링 (HTML/CSS/JS)
각각의 HTML 파일 내부에 작성되어 있던 인라인 코드들을 외부 파일로 분리하고 디렉토리 구조를 체계화했습니다.
- **코드 및 스타일시트 모듈화**: 중복을 제거하기 위해 Tailwind 설정을 `tailwind.config.js`로 분리하고, 공통 애니메이션 및 레이아웃 스타일을 `styles.css`로 추출하여 통합 관리합니다.
- **구조 재배치 및 성능 개선**:
  - `main.html`을 `index.html`로 승격하고 서브 페이지들은 `pages/` 폴더로, 자산들은 `css/`, `js/` 폴더로 나누어 웹 표준에 맞는 구조로 정비했습니다.
  - 모든 이미지에 `loading="lazy"`를 탑입하고, Google Fonts 최적화를 위해 `preconnect` 태그를 삽입하여 로딩 속도를 대폭 높였습니다.
  - 각 HTML 페이지마다 적절한 타이틀과 메타 태그를 적용하여 SEO를 개선했습니다.

## 🗄️ 2. 백엔드 PostgreSQL 및 SQLite Fallback 구축
- **안정적 RDBMS 스키마 설계**: `schema.sql`을 작성해 사용자, 상품, 원재료, 알러지 매핑 테이블 등 9개 테이블을 유기적으로 구조화하고 외래키 무결성을 잡았습니다.
- **Docker Compose 구성**: 로컬에 PostgreSQL 15가 컨테이너로 즉시 띄워지도록 세팅했습니다.
- **SQLite Fallback 지원**: PostgreSQL 연결 실패 시 로컬 파일 DB인 SQLite 환경으로 자동 대체되어 작동하는 이중화 구조를 적용했습니다.

## 🔍 3. 식약처 OpenAPI 연동 및 3단계 바코드 검색 흐름 구현
사용자가 바코드를 스캔하거나 입력 시, 실시간으로 식약처의 공식 오픈 API 및 로컬 DB를 연동하는 3단계 복구 검색을 구현했습니다.
1. **식약처 `I2570` API (1순위)**: 바코드 정보로 식약처 제품 정보 검색 및 품목보고번호 추출.
2. **식약처 `C005` API (2순위 Fallback)**: `I2570`에 데이터가 없을 때, 인허가/유통 데이터베이스인 `C005` API를 2차 질의하여 데이터 유실 방지.
3. **로컬 SQLite DB (3순위 Fallback)**: 공공 API에 존재하지 않는 테스트/씨드용 상품을 위해 로컬 데이터베이스를 최종 검색.
- **알러지 자동 판별 및 UI 표시**: 
  - `CertImgListServiceV3`를 호출하여 얻은 원재료 텍스트와 한국 식품 알레르기 유발물질 21종 키워드를 정밀 매칭합니다.
  - 로그인한 사용자의 프로필 알러지 정보와 매칭되는 성분이 검출되면 **빨간색 경고("섭취 불가")**, 안전하면 **초록색("안전한 식품")**으로 바코드 스캔 결과 화면에 시각적으로 강조합니다.
  - 스캔 결과가 아예 없는 경우 "잘못된 상품입니다" 에러 메시지를 사용자에게 표시합니다.

## 🛒 4. 장바구니(Cart) 및 주문 연동 시스템
- **로컬 스토리지 영속화**: `localStorage`를 사용하여 장바구니 항목과 수량이 세션 간에 보존되도록 구현했습니다.
- **실시간 알러지 경고 기능**: 장바구니에 상품을 추가하거나 주문(Checkout) 시, 사용자의 알러지 프로필과 마찰이 있는 상품이 포함되어 있다면 사용자에게 Alert 경고창을 띄워 안전을 환기합니다.
- **백엔드 주문 생성**: 주문 완료 시 NestJS 백엔드의 `POST /orders` API로 통신하여 로컬 DB에 영구 저장 및 동기화합니다.

## 👤 5. 회원 프로필 정보 수정 및 비밀번호 변경 기능
- **회원 정보/계정 설정 페이지 구현 (`profile_edit.html`)**:
  - 사용자 정보(아이디, 비밀번호, 이름, 이메일, 연락처, 생년월일, 성별, 본인인증 정보) 및 배송지 정보(수령인, 주소, 상세주소, 우편번호, 연락처)를 모두 수정 및 저장할 수 있는 입력 폼을 제공합니다.
  - 회원가입 시에도 위 항목을 모두 수집할 수 있도록 가입 로직을 함께 개편했습니다.
- **비밀번호 변경 삼중 검증**:
  - `[기존 비밀번호]`, `[새로운 비밀번호]`, `[새로운 비밀번호 재입력]` 3개 필드를 검증합니다.
  - 기존 비밀번호 불일치 시 "비밀번호가 틀렸습니다", 새 비밀번호 간 불일치 시 에러를 반환하며 정상 검증 완료 시에만 안전하게 비밀번호가 갱신됩니다.

## 🔒 6. 메인페이지 비로그인 대응 UI/UX 개선
- **알러지 정보 수정 버튼 동적 제어**: 
  - 로그인하지 않은 방문자의 경우 메인페이지에 "알러지 정보 수정" 버튼이 비노출되도록 수정했습니다.
  - 로그인한 유저에 한해 환영 메시지와 함께 알러지 수정 및 맞춤형 대시보드가 정상 표출됩니다.

## 📸 7. 바코드 스캔 결과 완료 화면 (`bacode_done.html`) 프리미엄 UI/UX 전면 개편
- **목업 시안에 기반한 디자인 개선**: 
  - 상단 헤더 및 하단 네비게이션 바 디자인 정비 (프리미엄 그린 테마 적용, 모던 아이콘 및 글래스모피즘 반영).
  - 결과 화면 카드 디자인 리뉴얼 및 우측 상단 플로팅 안전 배지(`SAFE`/`UNSAFE`) 탑재.
  - 경고 배너 및 내 프로필 맞춤 분석 영역을 2열 벤토(Bento) 그리드 구조로 개편하여 시각적 직관성 제공.
  - 성분 분석 영역에 원재료별 안전성 검사 상태에 따라 색상이 동적으로 바뀌는 알약(Pill) 모양 배지 및 상태 점(Dot)을 적용.
- **가상 알레르기 테스트 기능 추가**: 백엔드가 구동되지 않은 환경이나 오프라인 테스트 상황에서도 다양한 알레르기 케이스에 따른 레이아웃 작동 여부를 확인할 수 있도록 `mockAllergy` 쿼리 파라미터(예: `?mockAllergy=밀`)를 처리하는 로직을 개발 단계용으로 `js/auth.js`에 내장시켰습니다.

## 🚀 8. 대체 상품 추천 알고리즘 및 상품 검색/식별 구조 개선
- **알러지 별칭 확장 구조 도입 (`allergen_alias`)**:
  - 알러지원마다 여러 형태의 한글/영문 키워드(예: '우유', '계란', '달걀', 'egg' 등)를 등록할 수 있도록 `allergen_alias` 테이블을 정의하고, 서버 시작 시 21종 알러지 대상 키워드들을 자동 마이그레이션 및 시딩하도록 구축했습니다.
  - 외부 API 스캔 제품 및 전성분 분석 시 이 테이블을 기반으로 알러지 유무를 동적으로 검출하도록 고도화하였습니다.
- **상품 식별 구조의 확립 (id와 barcode 분리)**:
  - 쇼핑몰 내부 상품 상세 페이지는 데이터베이스 고유 일련번호인 `id`를 기준으로 조회하고, 바코드 스캔 시에는 `barcode`를 기준으로 개별 조회하도록 분리하여 식별 구조를 완전히 격리/안정화했습니다.
- **맞춤형 대체 상품 추천 알고리즘 구현**:
  - 동일한 `alternative_group`에 속한 안전한 대체 식품들을 찾아 정렬한 뒤, 사용자의 알러지 설정이 반영된 위험 식품을 자동 필터링하여 노출하는 SQL 추천 엔진을 설계하고 백엔드 API 및 프론트엔드 UI로 연동 완료했습니다.
- **최근 본 상품 로직 개편 및 로컬 스토리지 자동 청소 기능**:
  - 사용자가 쇼핑몰 상품 상세 페이지를 조회했을 때, **쇼핑몰 등록 상품(`source === 'shop'` 또는 `source === 'seed'`)이며 알러지 판별 결과 안전(`status === 'safe'`)한 건만** 중복 없이 로컬 스토리지 히스토리에 무제한 누적 저장되도록 개선했습니다.
  - 로그인 상태 변동 혹은 알러지 프로필 수정으로 인해 안전하지 않게 된 기존 방문 이력이나 레거시 외부 상품들을 메인페이지 및 전체이력 페이지 로드 시 **자동으로 감지하여 청소(Self-cleaning)하는 정밀 필터링**을 추가했습니다.
- **메인페이지 / 상세 페이지 기본 연동 값 오류 수정**:
  - 비로그인 상태일 때 기본으로 노출되는 아몬드 밀크의 매핑 타겟 `id`를 `1`(초코우유)에서 실제 DB 번호인 `4`로 수정하여 올바른 상품이 조회되도록 교정했습니다.

---

## ⚠️ 개발 생산성 및 오류 방지를 위한 핵심 교훈 (다음 작업 시 실수 방지용)

1. **로컬 서버 구동 환경 확인 시 주의점**:
   - 에이전트의 샌드박스 터미널 환경에서는 Windows 전역 `PATH`에 Node.js/npm이 등록되지 않아 `CommandNotFoundException`이 뜰 수 있습니다.
   - 이 경우, 시스템 내에 Visual Studio 혹은 다른 번들 경로에 설치된 Node 환경이 있는지 전체 검색하여 실행해야 합니다. (본 프로젝트의 경우 `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Microsoft\VisualStudio\NodeJs\node.exe`를 사용하여 해결).
   - Node.js 백엔드가 실행되지 않은 상황에서 파이썬 `http.server` 등 임시 정적 웹 서버를 구동할 경우, **`POST` 요청(예: `/auth/login`)이 501 Unsupported Method 에러**로 실패하므로 풀스택 동작 확인을 위해서는 반드시 Node.js 백엔드 서버를 구동해야 합니다.

2. **대규모 JS 코드 수정 및 리팩토링 시 주의점**:
   - `auth.js`와 같이 2000라인이 넘어가는 큰 파일의 일부분을 수정할 때는 중복 붙여넣기나 괄호 누락, trailing syntax error(예: `.json();` 코드 찌꺼기 남음 등)가 생기지 않도록 `StartLine`과 `EndLine` 범위 지정을 정교하게 확인해야 합니다.
   - 복잡한 코드 수정 후에는 항상 해당 파일에 구문 에러가 없는지 Node 컴파일 체크 등으로 사전에 확인하는 과정이 안전합니다.

---

## 🛍️ 9. 상품 DB 전면 교체 및 실데이터 기반 재구성

### 배경
기존 상품 DB에 더미/목업 데이터가 혼입되어 있어 전면 초기화 후 실제 한국 유통 상품 데이터로 재구성 작업을 진행했습니다.

### 주요 작업
- **식약처 `I2570` OpenAPI 기반 실상품 수집**: 알러지 대체 식품 카테고리별(유제품, 베이커리, 음료, 간식) 10개씩 총 40개 실제 유통 바코드 상품 데이터 수집
- **데이터 품질 보증**: 모든 상품에 실제 바코드, 브랜드명, 원재료, 이미지 URL, 알러지 매핑 포함 (누락 데이터 없음)
- **이미지 URL**: 식약처 HACCP 공식 인증 이미지 경로 사용 (`http://www.haccp.or.kr/...`)
- **DB 재시딩**: `seed.sql` 전면 재작성 및 SQLite DB 초기화 후 40개 상품 재시딩 완료

### 수집 카테고리 및 대표 상품
| 카테고리 | 상품 수 | 예시 |
|---|---|---|
| 간식/과자 | 10개 | 새우깡, 누드빼빼로, 초코파이 등 |
| 베이커리 | 10개 | 미니약과, 삼립호빵, 단팥빵 등 |
| 음료 | 10개 | 삼육두유, 코코팜, 소화제음료 등 |
| 유제품 | 10개 | 요구르트, 두유, 오트밀크 등 |

---

## 📄 10. 쇼핑몰 목록 페이지네이션 및 용량 옵션 선택 기능

### 구현 배경
- 쇼핑몰 상품 목록(`/shop`)에 상품이 한 페이지에 모두 나열되어 UX가 불편함
- 동일 상품의 용량/중량만 다른 SKU들(예: 두유 195ml / 200ml / 1000ml)이 각각 독립 카드로 노출되어 중복 혼란 발생

### 변경 파일 및 내용

#### [`backend/src/products/products.service.ts`](c:\Users\A\Desktop\20260909\backend\src\products\products.service.ts)
- **`extractBaseName(name)` 메서드 추가**: 상품명에서 용량 단위(`ml`, `g`, `l`, `ea` 등), 묶음 수량(`*3`, `x4`), 브랜드 접두사, 법인 표기(`(주)`, `㈜`) 등을 제거하여 동일 계열 상품을 묶는 정규화된 기본 이름 추출
- **`findById()` 개편**: 해당 상품과 동일 카테고리 내 `extractBaseName`이 일치하는 형제 상품들을 자동 조회하여 `options: [{ id, name, price, barcode }]` 배열로 API 응답에 포함

#### [`pages/shop.html`](c:\Users\A\Desktop\20260909\pages\shop.html)
- 상품 그리드(`#shop-product-grid`) 하단에 `<div id="shop-pagination">` 페이지네이션 컨테이너 추가

#### [`pages/shop_food.html`](c:\Users\A\Desktop\20260909\pages\shop_food.html)
- 상품 타이틀과 가격 사이에 `<div id="product-option-container">` 및 `<select id="product-option-select">` 드롭다운 컨테이너 추가
- 상품 이미지를 `object-contain`으로 변경하여 530×530 영역에 꽉 채우지 않고 비율에 맞게 축소 표시

#### [`js/auth.js`](c:\Users\A\Desktop\20260909\js\auth.js)
- **프론트엔드 `extractBaseName()` 추가**: 백엔드와 동일한 정규화 로직을 클라이언트에도 구현
- **`initShopPage` 리팩토링**:
  - 알러지 필터 후 노출 가능한 상품 리스트에서 `extractBaseName` 기준으로 중복 제거 → 대표 상품 1개만 카드 노출
  - 중복 제거된 고유 상품 목록을 `itemsPerPage = 9` 단위로 분할
  - `#shop-pagination`에 페이지 번호 버튼 동적 생성, 클릭 시 해당 페이지로 렌더링 + 최상단 스크롤
  - 카테고리/검색어 변경 시 `currentPage = 1`로 리셋
- **`renderProductDetail` 확장**:
  - API 응답의 `product.options`가 2개 이상일 때 `#product-option-container` 노출
  - 현재 상품에 해당하는 옵션에 `selected` 표시
  - 드롭다운 `change` 이벤트 발생 시 `/shop_food?id=[선택한 id]`로 즉시 이동(리다이렉트)

### 검증 결과
| 테스트 | 결과 |
|---|---|
| `GET /products` → 40개 상품, 카테고리별 10개 | ✅ |
| `GET /products/detail/15` → options에 미니약과 3종 (id: 15, 16, 17) | ✅ |
| `GET /products/detail/25` → options에 두유 3종 (id: 25, 26, 27) | ✅ |
| 백엔드 NestJS 서버 `http://localhost:3000` 정상 가동 | ✅ |

---

## ⚠️ 추가 교훈 (10번 작업 중 발견)

3. **`extractBaseName` 브랜드 패턴 순서 주의**:
   - 정규식에서 브랜드명은 **긴 이름 → 짧은 이름** 순으로 나열해야 합니다. 예: `삼립식품|삼립` 순서여야 `삼립식품`이 먼저 매칭되어 `식품`이 이름에 남는 오탐이 방지됩니다.
   - 잘못된 순서(`삼립|삼립식품`)로 배치 시 `식품`이 상품명에 잔류하여 baseName 비교 오류 발생.

4. **Unicode 깨짐 및 괄호 잔류 처리**:
   - 식약처 API에서 수신한 상품명에 `\uFFFD`(유니코드 대체 문자)나 `(주)`, `㈜` 등 법인 표기 잔류가 발생할 수 있습니다.
   - `replace(/[\uFFFD]/g, '')`, `replace(/\([^)]\)/g, '')` 등의 클리너를 우선 처리하여 정규화 로직의 신뢰도를 높여야 합니다.

---

## 🗺️ 11. 주변 안전 음식점 지도 기능 개발 및 카카오 API 연동 예외 가드 구축

### 구현 배경
- 로그인된 사용자의 위치를 기반으로 반경(50m, 100m, 200m) 내의 음식점을 조회하고, 유저의 알러지 성분이 포함되었는지 진단하여 지도상에 시각화(초록/노랑/빨강 마커)하는 지도 서비스를 구축했습니다.
- 로컬 개발 환경(`http://localhost:3000`) 접속 시, 카카오 개발자 센터의 도메인 미등록 등으로 카카오 Maps SDK 로딩에 실패할 경우 `kakao is not defined` 스크립트 에러가 터지고 위치 검색, GPS 버튼 등 웹페이지 전반이 먹통이 되는 문제를 보완했습니다.

### 변경 파일 및 개선 내용

#### [`pages/map.html`](file:///c:/Users/A/Desktop/AllergyFree-main/pages/map.html)
- **지도 라이브러리 예외 가드 추가**: `initMapPage()` 진입 시 `window.kakao`의 존재 여부를 1차 검증하여 SDK 미로드 시 오류 크래시 없이 우아하게 중단되도록 가드 코드를 매핑했습니다.
- **친절한 에러 안내 UI 렌더링**: 카카오 SDK 로딩 실패 시 지도 영역(`div#map`)에 "지도 라이브러리 로드 실패" 경고 패널과 함께 카카오 개발자 센터로 이동할 수 있는 가이드 버튼을 렌더링합니다. 사이드바에도 분석 불가능 상태를 안내하는 에러 카드를 동적으로 띄웁니다.
- **버튼 Fallback 기능 연동 (`setupFallbackMapDom`)**: SDK가 로드되지 않은 상태에서 위치 검색 입력 및 버튼, GPS 트래커 버튼(`sidebar-gps-btn`, `mobile-gps-btn`)을 누를 시, 자바스크립트 예외 오류가 콘솔에 찍히며 아무 반응이 없던 현상 대신 **"카카오 개발자 센터에서 도메인 등록이 필요하다"는 경고 팝업 얼럿**을 띄우는 Fallback 리스너를 바인딩했습니다.
- **다중 페이지 스캔 및 정밀 하버사인(Haversine) 필터링 도입 [NEW]**: 카카오 로컬 API가 1페이지당 최대 15개 결과만 제한하여 제공하는 특성 때문에 100m, 200m로 반경을 넓혀도 추가 식당이 보이지 않던 문제를 수정했습니다. 선택 반경에 따라 다중 페이지(50m: 1페이지/15개, 100m: 2페이지/30개, 200m: 3페이지/45개)를 스캔한 후, 중복을 제거하고 중심 좌표 기준의 실거리를 하버사인 공식으로 정확히 연산 및 필터링하여 오름차순 정렬해 백엔드에 제공합니다. 이제 반경 토글 시 주변의 더 넓은 영역의 식당들이 정상적으로 누적 추가되어 목록과 지도에 출력됩니다.
- **카카오 Circle 객체 setCenter TypeError 해결 [NEW]**: 카카오 지도 SDK의 `Circle` 객체 스펙에 존재하지 않는 `setCenter(latLng)` 메서드를 호출하여 지도를 이동하거나 반경을 변경할 때 자바스크립트 예외(`radiusCircle.setCenter is not a function`)가 터지고 후속 스캔(`triggerPlacesSearch()`)이 정지되던 근본적인 버그를 수정했습니다. 이제 `setOptions({ center: latLng, radius: selectedRadius })`를 사용하여 정상적으로 원의 중심 좌표와 반경을 업데이트합니다.
- **식당명 및 카테고리 필드명 불일치 해결 (undefined 문제 조치) [NEW]**: 백엔드 API 응답 필드명(`place_name`, `category_name`)과 프론트엔드가 참조하는 필드명(`name`, `category`)의 불일치로 인해 화면상에 식당명과 업종명이 `undefined`로 렌더링되던 현상을 해결했습니다. 백엔드 응답을 받은 즉시 프론트엔드 친화적인 이름으로 매핑하는 어댑터 로직을 도입하여 정상적으로 식당 이름과 카테고리가 렌더링됩니다.
- **주소 및 키워드 장소 이중 검색 폴백(Fallback) 연동 [NEW]**: 위치 검색창에 "서울대학교", "강남역" 등 지번/도로명 주소가 아닌 랜드마크 키워드 입력 시 "주소 결과가 존재하지 않습니다"라며 검색에 실패하던 한계를 수정했습니다. 1차 주소 검색(Geocoder) 실패 시 자동으로 2차 카카오 키워드 검색(Places KeywordSearch)을 호출하여 연동하도록 이중 검색 시스템을 탑재했습니다. 검색 성공 시 장소명을 자동으로 깔끔하게 보정(Autocomplete)하고 해당 장소의 좌표로 지도를 이동하여 정상적으로 주변 안전 음식점을 탐색합니다.

#### [`index.html`](file:///c:/Users/A/Desktop/AllergyFree-main/index.html)
- **성분 스캐너 카드 링크 연동 [NEW]**: 메인페이지의 벤토 그리드 영역에서 "성분 스캐너" 카드를 클릭했을 때 페이지 이동이 누락되어 있던 부분을 수정하여, 클릭 시 스캔 페이지(`/scan`)로 정상 리다이렉트되도록 `onclick` 이벤트를 바인딩했습니다.

#### [`js/auth.js`](file:///c:/Users/A/Desktop/AllergyFree-main/js/auth.js)
- `triggerPageSpecificInit()` 내의 지도 로딩 함수 `initMapPage()` 호출 부를 `try-catch`로 묶어 예외 차단 기능을 보강했습니다.

#### [NEW] [테스트 스크립트 (`test_kakao_api.js`)](file:///C:/Users/A/.gemini/antigravity-ide/brain/ea81feca-04d3-47ae-820f-b331a6152ecb/scratch/test_kakao_api.js)
- 카카오 API 서버의 정상 작동 여부와 백엔드의 플레이스 크롤링 송수신 상태를 독립적으로 검증하기 위해 Node.js 연결 테스트 스크립트를 작성하여 테스트를 수행했습니다.
- **검증 결과**:
  1. **Web SDK**: Referer 도메인이 미등록되어 카카오 서버가 `401 Unauthorized`를 내뱉는 것을 확인했습니다. (즉, 사용자 카카오 콘솔의 Web 플랫폼 도메인 설정에 `http://localhost:3000`을 추가하면 즉시 해소됨을 증명)
  2. **크롤링 API**: 카카오의 상세 웹페이지 개편으로 비공식 내부 API(/main/v/{id})가 봇 차단 상태(404)임을 감지했습니다.
  3. **백엔드 Fallback 정상 동작 확인**: 크롤링이 거부되더라도, 백엔드(`restaurants.service.ts`)의 설계상 **가게의 카테고리 정보(예: 중식, 베이커리 등)를 활용한 Fallback 알러지 유해성 진단 로직이 내장되어 있어**, 도메인 등록만 마치면 정상적으로 지도의 안전성 식당 마커 핀들이 출력되고 기능이 작동함을 검증했습니다.